"use client";

/** LEGACY — REVIEW LATER: form unused until Admin Panel phase; mutations go through Django. */
export function ProductEditorForm() {
  return (
    <p className="text-sm text-muted">
      Product editor will be rebuilt in the Admin Panel phase against Django catalog APIs.
    </p>
  );
}
